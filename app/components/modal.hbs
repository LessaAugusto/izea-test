{{#if @isVisible}}
  <div class="modal" data-test-modal-container>
    <div class="content">
      <div class="header">
        <button class="close-button" {{on "click" @onClose}} data-test-close-button />
      </div>
      {{yield}}
    </div>
  </div>
{{/if}}